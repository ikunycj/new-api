interface:
  display_name: "Deploy new-api"
  short_description: "Deploy the latest new-api master safely"
  default_prompt: "Use $deploy-new-api to select a host, verify the latest origin/master, and safely deploy new-api on the existing 1Panel stack."
