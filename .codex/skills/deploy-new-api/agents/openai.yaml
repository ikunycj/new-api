interface:
  display_name: "Deploy new-api"
  short_description: "Deploy new-api and its APISIX gateway safely"
  default_prompt: "Use $deploy-new-api to select aliyun, alltokenapi, or ikun.love, verify the requested application or APISIX gateway scope, and deploy it with independent validation and rollback."
